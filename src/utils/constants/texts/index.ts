import {
  start,
  login,
  forgotPassword,
  confirmResetCode,
  resetPassword,
} from "./screens";

export const Text = Object.freeze({
  start,
  login,
  forgotPassword,
  confirmResetCode,
  resetPassword,
});
