import {
  start,
  login,
  forgotPassword,
  confirmResetCode,
  resetPassword,
  authenticated,
} from "./screens";

export const Text = Object.freeze({
  start,
  login,
  forgotPassword,
  confirmResetCode,
  resetPassword,
  authenticated,
});
